{
  "name": "weredev.com",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "node-sass --include-path scss scss/ -o wwwroot/css/",
    "build": "npm run build-css"
  },
  "devDependencies": {
    "node-sass": "^4.11.0"
  }
}
